-- Program-level emissions impact for the 2024 VSR season.
-- One row: actual, counterfactual and impact totals across three spatial
-- domains (global, inside VSR zones, outside VSR zones).
SELECT *
FROM `emlab-gcp.ocean_ghg_bwbs.bwbs_program_impact`
